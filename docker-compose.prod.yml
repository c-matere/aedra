version: "3.9"

# Production overrides for docker-compose.yml
# Currently no service-specific overrides; keeping a stub file so scripts that reference
# `-f docker-compose.prod.yml` continue to work.

services: {}
